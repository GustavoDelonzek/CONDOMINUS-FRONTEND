import { FileText } from 'lucide-react';
import { PlaceholderPage } from '../../components/ui/PlaceholderPage';

export function Documents() {
  return (
    <PlaceholderPage
      icon={FileText}
      title="Documents"
      description="Central de documentos e contratos das administradoras."
    />
  );
}
